# Harborline desk

Next.js 15 app for the supplier performance workstation. Generate `public/data/dashboard.json` from the repo root with `PYTHONPATH=. python3 scripts/run_pipeline.py` before the first run if the extract is missing.

```bash
npm install
npm run dev -- --port 43147 --hostname 127.0.0.1
```
